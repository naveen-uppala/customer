name: customer CI Pipeline

on:
  workflow_dispatch: 

jobs:
  BUILD:
    runs-on: ubuntu-latest
    steps:
      - name: Clone Repository
        uses: actions/checkout@v4
        
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4  
        with:
          aws-access-key-id: 
          aws-secret-access-key: 
          aws-region: 
        
